name: Java CI with Gradle

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '18'
          distribution: 'temurin'
          cache: gradle
      - name: Build with Gradle
#        env:
#          repo-name: ${{ secrets.SITE_PUSH_REPO_NAME }}
        with:
#          repo-name: ${{ secrets.SITE_PUSH_REPO_NAME }}
          arguments: :displayParam -q -Psite_push_repo_name=${{ secrets.SITE_PUSH_REPO_NAME }} # -Psite_push_repo_url=${{ repo-url }} -Psite_push_repo_username=${{ repo-username }} -Psite_push_repo_password=${{ repo-password }}
          gradle-version: 7.5.1
        uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
