apiVersion: v1
kind: Secret
metadata:
  name: organization-postgresql
  namespace: default
  labels:
    app: organization-postgresql
type: Opaque
data:
  postgresql-password: N2E3NTc0YmNkMTZkYTAwZTJlZTIwMGY1MTZlM2QwNzAwY2FhMmQ5N2JiMWQzZjY4Y2JlOWNiM2MxYTM3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: organization-postgresql
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: organization-postgresql
  template:
    metadata:
      labels:
        app: organization-postgresql
    spec:
      volumes:
        - name: data
          emptyDir: {}
      containers:
        - name: postgres
          image: postgres:14.2
          env:
            - name: POSTGRES_USER
              value: organization
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: organization-postgresql
                  key: postgresql-password
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          resources:
            requests:
              memory: '512Mi'
              cpu: '500m'
            limits:
              memory: '1Gi'
              cpu: '1'
---
apiVersion: v1
kind: Service
metadata:
  name: organization-postgresql
  namespace: default
spec:
  selector:
    app: organization-postgresql
  ports:
    - port: 5432
