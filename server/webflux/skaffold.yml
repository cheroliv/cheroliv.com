apiVersion: skaffold/v1
kind: Config
build:
  artifacts:
    - image: organization
      context: ../webflux
      jib: {}
  tagPolicy:
    envTemplate:
      template: '{{.IMAGE_NAME}}:latest'
deploy:
  kustomize: {}
